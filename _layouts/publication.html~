---
layout: default
---

<article>
  <h1>{{ page.title }}</h1>
  <p><strong>Authors:</strong> {{ page.authors }}</p>
  <p><strong>Year:</strong> {{ page.year }}</p>
  <p><strong>Journal:</strong> {{ page.journal }}</p>
  <p><strong>Volume:</strong> {{ page.volume }}</p>
  <p><strong>Issue:</strong> {{ page.issue }}</p>
  <p><strong>Pages:</strong> {{ page.pages }}</p>
  {% if page.url %}
  <p><a href="{{ page.url }}" target="_blank" class="btn btn-primary">View on Journal</a></p>
  {% endif %}
</article>
